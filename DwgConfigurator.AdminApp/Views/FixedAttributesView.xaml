<UserControl x:Class="DwgConfigurator.AdminApp.Views.FixedAttributesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DwgConfigurator.AdminApp.ViewModels">
    <UserControl.DataContext>
        <vm:FixedAttributeViewModel />
    </UserControl.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Filtri" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal" Margin="8">
                <TextBlock Text="Gamma:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0"/>
                <ComboBox ItemsSource="{Binding ProductGroups}" SelectedItem="{Binding SelectedProductGroup}" DisplayMemberPath="Name" Width="260" Height="30" Margin="0,0,14,0"/>
                <TextBlock Text="Modulo:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0"/>
                <ComboBox ItemsSource="{Binding ModuleOptions}" SelectedItem="{Binding SelectedModuleOption}" Width="260" Height="30" Margin="0,0,14,0"/>
                <TextBlock Text="Applica a:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,6,0"/>
                <ComboBox ItemsSource="{Binding AppliesToOptions}" SelectedItem="{Binding SelectedAppliesTo}" Width="150" Height="30"/>
            </StackPanel>
        </GroupBox>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Attributes}"
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  Margin="0,0,0,8"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  EnableRowVirtualization="False">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="MinHeight" Value="30"/>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Tag" Binding="{Binding Tag}" Width="260" IsReadOnly="True"/>
                <DataGridTemplateColumn Header="Valore" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Value}"
                                       TextWrapping="Wrap"
                                       Padding="6,4"
                                       VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Auto"
                                     MinHeight="34"
                                     Padding="4"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="💾 Salva su Gamma/Modulo" Command="{Binding SaveCommand}" Width="200" Height="32" Margin="0,0,8,0"/>
            <Button Command="{Binding RefreshCommand}" Width="135" Height="32">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="⟳" FontSize="15" RenderTransformOrigin="0.5,0.5" Margin="0,0,7,0">
                        <TextBlock.RenderTransform>
                            <RotateTransform Angle="0"/>
                        </TextBlock.RenderTransform>
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRefreshing}" Value="True">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard x:Name="SpinStoryboard">
                                                <Storyboard RepeatBehavior="Forever">
                                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.Angle" From="0" To="360" Duration="0:0:0.75"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <RemoveStoryboard BeginStoryboardName="SpinStoryboard"/>
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBlock Text="{Binding RefreshButtonText}"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
